class Entry {
    constructor(sample, name, start, end, points, extra) {
        this.sample = sample
        this.name = name
        this.start = start
        this.end = end
        this.points = points
        this.extra = extra
    }
}
