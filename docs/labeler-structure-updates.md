## Update History of Labeler Structure

Please use the number after `v` as the `serialVersion` of your labeler.

Latest serial version: `3`

#### v3
 - Introduce `moduleManagement` to define module (subproject) management operations (add/rename/remove/duplicate)
   that are shown in the application's `File` menu.

#### v2
 - Introduce directory structured labeler.

#### v1 and before
Omitted.
