---
sidebar_position: 1
title: 自定义应用目录
---

# 自定义应用目录

vLabeler 将用户相关的数据（如自定义的标注器、插件、应用设置和日志）保存在应用目录中。默认情况下，该目录为 `~/vLabeler`（macOS 上为 `~/Library/vLabeler`）。

您可以在启动应用前将环境变量 `VLABELER_APP_DIR` 设置为一个目录的绝对路径，来更改该位置。如果该目录不存在，将会被自动创建。如果该值无效，将使用默认位置。

请注意，现有内容不会被自动迁移。如果您想保留当前的设置，请手动将原应用目录中的内容复制到新位置。
